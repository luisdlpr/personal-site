<script>
	export let progress;
	const angle = 360 * progress;

	// Adapt the logic according to the approach
	const background = `radial-gradient(#131822 50%, transparent 51%),
    conic-gradient(transparent 0deg ${angle}deg, #131822 ${angle}deg 360deg),
    white;`;

	$: cssVarStyles = `--background:${background}`;
</script>

<div id="progress-circle" style={cssVarStyles} />

<style>
	#progress-circle {
		background: var(--background);
		border-radius: 50%;
		width: 120px;
		height: 120px;
		transition: all 500ms ease-in;
		will-change: transform;
	}
</style>
